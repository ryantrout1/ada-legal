/**
 * DiningSurfaceDiagram - stub placeholder for Commit 2 of the Standards Guide migration.
 * Replaced with the real interactive SVG in Commit 3.
 *
 * See DiagramPlaceholder for the shared rendering and the rationale.
 */

import DiagramPlaceholder from './DiagramPlaceholder.js';

export default function DiningSurfaceDiagram() {
  return <DiagramPlaceholder name="Dining Surface" />;
}

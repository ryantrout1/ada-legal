/**
 * Single import surface for the schema.
 *
 *   import * as schema from '@/db/schema';
 *   import { adaSessions, organizations } from '@/db/schema';
 */

export * from './schema-core';
export * from './schema-ch1';

# Regression Ledger — ADA Legal Link (ada-legal)

Append-only backlog of bugs that were diagnosed to root cause, fixed,
and pinned with a permanent test. Never edit or delete rows; corrections
get a new note row. Newest at the bottom. Per /regress.

| Date | Bug | Origin | Test | Commit |
|---|---|---|---|---|
| 2026-07-01 | Admin litigation POST/PATCH uppercased affected_states without stripping the `__nationwide__` sentinel — one admin save away from storing `__NATIONWIDE__`, which the case-sensitive read filter passes through to the public page and Ada's prompt (17 live sentinel rows at diagnosis, 0 corrupted) | /triage "__nationwide__ renders as code" → /fixit ada-legal `b4aa0ac` + B44 `6d018cc` | tests/unit/normalizeAffectedStates.test.ts (sanitizeIncomingStates block) | _pending_ |
| 2026-07-07 | Public `/api/attorneys` was the only public GET endpoint that never called `applyCors`; the directory page on adalegallink.com fetched it cross-origin, the browser blocked the 200 response ("No Access-Control-Allow-Origin header"), and the page rendered "couldn't load" with the full roster (Kelley + firm) present in the body. Latent since Phase B; not the domain lockdown | /triage "Kelley's firm not listed" → /fixit `de802aa` | tests/integration/attorneysCors.test.ts | `de802aa` |
| 2026-07-27 | Migration 0024 re-added `'mass'` to the `litigation_listings.kind` CHECK and nothing in TypeScript learned about it. Seven places wrote the kind list out by hand; five of them lacked `mass`, so a mass row rendered its raw slug on the public page, was filtered out of the public directory entirely (`VALID_KINDS` derived from the incomplete `KIND_ORDER`), hit a `?? 'litigation'` fallback in Ada's prompt, rendered blank in the admin edit dropdown (a `<select>` whose value matches no `<option>`), and was silently dropped by the admin write guard on save — so the screen was wrong and the stored data was right, which is why nothing surfaced. No `mass` rows existed until Disney was corrected on 2026-07-27, at which point all five fired at once | /shipit taxonomy Phase 1, then /shipit admin Phase 1 after the admin copies surfaced | tests/unit/adminLitigationKind.test.ts — walks `LITIGATION_KINDS` and asserts every value has a label AND passes the write guard, and that the two sets are identical | `da9f232` + `9bae994` |
| 2026-07-27 | `/portal/litigations` decided its badge, its grouping and its type filter with `kind === 'class' ? 'Class action' : 'Mass action'`. Six kinds exist, so all 22 non-class records — 8 DOJ enforcement actions, 10 pattern-of-practice, 2 consent decrees, 2 regulatory challenges — were labelled and filed as mass actions. Not cosmetic: accepting sets `receivesMatches`, which is what makes `resolveEligibleRoutingFirm` hand a firm an EXCLUSIVE routed lead, so a firm could opt into exclusive routing for a DOJ investigation with no claimants or a pattern-of-practice record with no court and no docket. The identical ternary was fixed in Ada's prompt a month earlier and that fix was scoped to that file — nothing pointed here. Seventh copy of the same list; found because Ryan opened the page, not by any check | /triage \"portal litigation labels\" → /fixit | tests/unit/portalLitigationLabels.test.ts — source assertions naming the old patterns (no React render testing exists in this repo) | `15f681a` |
| 2026-07-27 | `--color-accent-500` and `--color-ink-500` were set back to Base44's AA-level values on 2026-07-24 so the rebuilt site could be compared like-for-like against production during the decommission. Recorded as dated debt in `tokenParity.test.ts`, with the shortfall asserted to two decimal places. The comparison finished and the darkening was restored — but two comments in `app.css` had claimed all along that the file held a darkened value (one named `#883D17`, which it never held), and the project instructions listed a "corrected" `accent-600 #9C340A` that computes to 6.53 and would not have cleared either. Ten routes were failing the AAA sweep | /triage \"accent contrast failures\" — diagnosed as NOT a defect, then the trade was retired on Ryan's call | tests/unit/tokenParity.test.ts — exact ratios asserted, no AAA exemptions remain | `c9e08dd` |
| 2026-07-27 | `--color-ada-400` (`#7C5CFC`) was used as the landing CTA fill with white text, measuring 4.38:1 — below even the AA floor of 4.5, and outside the recorded token debt, which justified itself on the grounds that accent-500 "still clears AA". The token's own comment says it is the DARK-panel tier; the button renders on a light card. Fixed by pointing `--v2-ada` at `ada-500`, the light-surface tier that already existed | Found during the AAA sweep triage; not covered by the debt note | tests/unit/tokenParity.test.ts (token sweep, no exemptions) | `c9e08dd` |
| 2026-07-27 | The Ada transcript carried `aria-label` on a bare `<div>` with no role. `aria-label` is discarded on an element with no valid role, so the live region was unnamed to a screen reader — the only finding in that sweep that broke something rather than made it harder to read. Both the public chat and the admin listing preview | Found during the AAA sweep triage | covered by the a11y route sweep (`/ada` passes) | `c9e08dd` |
| 2026-07-27 | Commit `b783e7b` (2026-07-23) rebuilt `/attorneys` to Base44's design and removed the `<fieldset>` and `<legend>` wrapping the filter controls, along with the practice-area `role="switch"` chips. The pre-rebuild file's own header listed all three as deliberate improvements over B44. axe never flagged it — a missing fieldset is an absent affordance, not a violation — so the page kept passing its accessibility sweep for weeks while two Playwright tests reported it correctly and were read past. Same commit and same week as the token debt; that trade was recorded, this one was silent | /triage \"attorney filter tests failing\" → /fixit Part 1 | tests/a11y/attorneys-filter-states.spec.ts (pre-existing; it was right all along) | `2403fc4` |
| 2026-07-27 | `updateLitigation` in `inMemoryClients` never learned `barrierCategory` or `intakeStatus`. They were wired into the Neon client in taxonomy Phase 1 and its twin was missed, so both fields round-tripped correctly in production and silently did nothing in every test using the in-memory client — a test could have asserted a category was set, passed, and proved nothing. Latent for four phases | Caught by the Phase 2 admin tests, not by anything that existed | tests/unit/adminLitigationTaxonomy.test.ts (round-trip assertions) | `e902692` |
| 2026-07-27 | `buildLitigationMatchedListing` resolved a contact firm only from our own `law_firms` network and returned `null` when none resolved, which drops the readout to a generic class-action placeholder naming nobody. Class counsel on a class action is usually a firm we have no row for. Went live on Niles the moment its lead attorney and firm assignment were removed (2026-07-27) after the assigned firm turned out not to be counsel on the case — a wheelchair user describing a Hilton bed-transfer problem got a case summary and no way to reach anyone. Overturned a recorded decision: two tests asserted null on the reasoning that a placeholder was "honest", true while those were the only two options | /plan class-action-match Phase 1 → /shipit | tests/unit/litigationMatchedContact.test.ts — class counsel preferred, never null for a real case, `contactIsClassCounsel` distinguishes counsel of record from a firm that merely takes related work | `094e0a5` |
| 2026-07-27 | `decideLane` never read the matched litigation's `kind`, so a class action and a pattern-of-practice record took identical paths. A firm could accept a class action and receive an EXCLUSIVE routed lead it could do nothing with — a person who fits a certified class is already in it, there is nothing to enrol them in, and a firm that is not appointed class counsel cannot act on the class claim — while that lead stayed invisible to every other firm, because exclusivity is the point of `routed_firm`. Root cause of the whole class-action problem: the system could not tell the difference between a case somebody can be handed and a case they are already inside | /plan class-action-match Phase 3 → /shipit | tests/unit/routeCase.test.ts — class lands in `class_member` with the firm still attached for a separate claim, and every other kind keeps its existing path | `787547f` |
